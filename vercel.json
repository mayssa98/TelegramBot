{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["iad1"],
  "fluid": true,
  "functions": {
    "api/webhook.py": {
      "maxDuration": 60,
      "regions": ["iad1"]
    }
  },
  "crons": [
    {
      "path": "/api/cron/restock",
      "schedule": "*/10 * * * *"
    }
  ]
}
